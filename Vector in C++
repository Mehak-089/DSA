// Online C++ compiler to run C++ program online
#include <iostream>
#include<vector>
using namespace std;
int main() {
    // pair<int,int>p={1,3};
    // cout<<p.first<<" "<<p.second<<endl;
    // pair<int,int>arr[]={{1,3},{1,4},{4,6}};
    // cout<<arr[0].second;
    vector<int>v={3,4,6,7,2,3};
    v.push_back(2);
    v.emplace_back(5);
    v.push_back(9);
    for(int i =0 ; i<v.size();++i){
        cout<<v[i]<<" ";
    }
    cout<<endl;
    for(vector<int>::iterator it=v.begin();it!=v.end();it++){
        cout<< *(it)<<" ";
    }
    cout<<endl;
    for(auto it=v.begin();it!=v.end();it++){
        cout<<*(it)<<" ";
    }
    cout<<endl;
    for(auto it:v){
        cout<<it<<" ";
    }
    // }
    // vector<pair<int,int>>vec;
    // vec.push_back({1,2});
    // vec.emplace_back({2,3});
   
    

    return 0;
}
